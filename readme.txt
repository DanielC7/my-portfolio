Run localy: node app.js

git add .
git commit -m 'commit message'
git push -u origin master (push to github)

heroku login (log in to heroku CLI if session expired)
git push heroku master (push to Heroku)